This directory contains various weird documents meant to testing, if we're
parsing and rendering them properly. Frequently, documents are getting added
here when we implement feature used there or bug occuring inside these
documents.

Possible meanable content of the documents is completely irrelevant and you
should ignore it. Noone takes any responsibility for it :).
